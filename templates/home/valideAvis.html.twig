{% extends "base.html.twig" %}


{% block body %}

{% for avis in avisListe %}
						<article>
							<h2>
								{{ avis.film.titre}}
							</h2>
							<div class="content">

					<img src="{{ asset('uploads/images/' ~ avis.film.idImage) }}" alt="{{ avis.film.titre }}">

            
                    <div class="comment">
				<div class="row">
				<div class="col-3">
					{{avis.utilisateur.prenom}} {{avis.utilisateur.nom}} 
					<br>
					Note donnée : {{avis.note}} / 5
				</div>
				<div class="col">
				{{avis.description | raw }}
				</div
				</div>
				</div>

					<a href="{{ path('avis_valide', {'id': avis.id}) }}" class="btn btn-primary"> Valider cet avis</a>
                        <a href="{{ path('avis_delete', {'id': avis.id}) }}" class="btn btn-primary"> Supprimer cet avis</a>

							</div>
						</article>


{% endfor %}

{% endblock %}
