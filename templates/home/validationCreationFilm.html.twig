{% extends "base.html.twig" %}

{% block body %}

<figure>
  <blockquote class="blockquote">
    <p class="mb-0">Vous venez d'enregistrer un nouveau film !</p>
  
  </blockquote>
</figure>
    <h1> </h1>

    <div class="d-grid gap-2">
  <a href="{{ path('film_create')}}" class="btn btn-lg btn-primary">Enregister un nouveau film</a>
  <a href="{{ path('film_edit', {'id': film.id}) }}" class="btn btn-lg btn-primary">Modifier le dernier film enregistré </a>
  <a href="{{ path('home')}}" class="btn btn-lg btn-primary">Page d'acceuil</a>
  <a href="{{ path('film_show', {'id': film.id}) }}" class="btn btn-lg btn-primary">Voir le film</a>
</div>
{% endblock %}